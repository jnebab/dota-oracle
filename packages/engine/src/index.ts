// @dota-oracle/engine — pure, deterministic scoring + item-build engine (no React, no IO).
// M1 ports scoreHero / tagEdges / buildGuide / situationalItems out of the prototype.
export const ENGINE_VERSION = "0.0.0-scaffold";
